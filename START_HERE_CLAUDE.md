# First Claude Prompt

Paste this into Claude Code from the repository root:

> Read MASTER.md, AGENTS.md, CLAUDE.md, BUILD_PLAN.md and the docs directory. Do not build beyond Phase 0. Inspect the starter repository, install/configure the current Cloudflare vinext integration, preserve the existing project rules, and get the app running locally against local Cloudflare bindings. Do not connect local development to the production `lucy` D1. Report every generated/changed file and any Wrangler values I must fill in manually. Stop once Phase 0 local development is working and verified.

After Phase 0 is complete, start Phase 1A in a separate task.
