# Authentication layer

Phase 1A integrates a maintained Cloudflare/D1-compatible authentication library.

Current preferred direction: Better Auth.

Do not implement custom password hashing, session tokens or email verification logic unless the master architecture is explicitly changed.
